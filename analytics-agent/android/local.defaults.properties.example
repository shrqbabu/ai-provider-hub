# Copy to local.defaults.properties (git-ignored).
# PUBLIC VALUES ONLY — anything secret belongs on the analytics backend.
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_PUBLISHABLE_KEY=<anon / publishable key>
ANALYTICS_API_URL=https://analytics-api.yourcompany.com
